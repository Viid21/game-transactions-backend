// use prisma-next

model Product {
  id           String @id
  name         String
  description  String?
  priceInCents Int
  currency     String

  createdAt    TimestamptzString @default(now())
  updatedAt    temporal.updatedAtString()
}